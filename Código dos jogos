#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

/* run this program using the console pauser or add your own 
getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
    int menu;
    int A;
    int B;
    int C;
    int D;
    char alternativa;
    int v;
    char escolhafinal;
    char P;
    int g1; 
    int g2;
    int g3;
    int g4;
    char atitude;
    char atacante;
    char atacado;
    int doador;
    int transferidos;
    char maquina;

    do {
        do {
            printf("GAMEZATION:\n");
            printf("Escolha o jogo-opcao desejada:\n");
            printf("1.Perguntas e respostas\n");
            printf("2.Cobra na caixa\n");
            printf("3.Gousmas war\n");
            printf("4.Sair\n");
            scanf("%d", &menu);

            A = menu == 1;
            B = menu == 2;
            C = menu == 3;
            D = menu == 4;

            if(menu != 1 && menu != 2 && menu != 3 && menu != 4) {
                printf("Item selecionado é inexistente, tente novamente\n");
                while(getchar()!= '\n');
                menu = 0;
            }
        } while(menu != 1 && menu != 2 && menu != 3 && menu != 4); // Terminar de programar o menu para voltar quando digitarem algo fora do comum.
        
        if(D == 1 ) { //Está certo? 
            printf("saindo do jogo");
            return 0;
        }
        
        if(A == 1) {
            do {
                do {
                    printf("A franquia de God of War tem quantos jogos principais, cosiderando as sequencias e as prequels?\n a)12 \n b)3 \n c)8\n d)5\n");
                    scanf(" %c", &alternativa);
                    if(alternativa == 'c') {
                        printf("Resposta correta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'b') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'a') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'd') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa != 'a') {
                        if(alternativa != 'b') {
                            if(alternativa != 'c') {
                                if(alternativa != 'd') {
                                    printf("Resposta invalida, tente novamente\n");
                                    printf("==================================\n");
                                }
                            }
                        }
                    }
                } while(alternativa != 'a' && alternativa != 'b' && alternativa != 'c' && alternativa != 'd');

                do {
                    printf("Qual o protagonista do anime Bleach?\n a)Rukia\n b)Ichigo\n c)Neymar\n d)Midorya\n");
                    scanf(" %c", &alternativa);
                    if(alternativa == 'b') {
                        printf("Resposta correta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'a') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'c') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'd') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa != 'a') {
                        if(alternativa != 'b') {
                            if(alternativa != 'c') {
                                if(alternativa != 'd') {
                                    printf("Resposta invalida, tente novamente\n");
                                    printf("==================================\n");
                                }
                            }
                        }
                    }
                } while(alternativa != 'a' && alternativa != 'b' && alternativa != 'c' && alternativa != 'd');

                do {
                    printf("O que e essencial em um pitch?\n a)Falar muito sobre si mesmo\n b) Falar sobre o problema de interesse \n c)Mostrar o projeto de forma detalhada\n d)Elogiar os compradores\n");
                    scanf(" %c", &alternativa);
                    if(alternativa == 'b') {
                        printf("Resposta correta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'a') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'c') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'd') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa != 'a') {
                        if(alternativa != 'b') {
                            if(alternativa != 'c') {
                                if(alternativa != 'd') {
                                    printf("Resposta invalida, tente novamente\n");
                                    printf("==================================\n");
                                }
                            }
                        }
                    }
                } while(alternativa != 'a' && alternativa != 'b' && alternativa != 'c' && alternativa != 'd');

                do {
                    printf("Qual o nome do(a) professor(a) de ALGORITMO E CODIFICACAO DE SISTEMAS? \n a)RENATA NOVAES\n b)POLYANA SANTOS\n c)BENJAMIN YUJI\n d)PEDRO GIROTTO\n");
                    scanf(" %c", &alternativa);
                    if(alternativa == 'd') {
                        printf("Resposta correta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'b') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'c') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'a') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa != 'a') {
                        if(alternativa != 'b') {
                            if(alternativa != 'c') {
                                if(alternativa != 'd') {
                                    printf("Resposta invalida, tente novamente\n");
                                    printf("==================================\n");
                                }
                            }
                        }
                    }
                } while(alternativa != 'a' && alternativa != 'b' && alternativa != 'c' && alternativa != 'd');

                do {
                    printf("Qual o(s) pet(s) do Girotto?\n a) gatos \n b) cachorros \n c)Iguana\n d)Porquinho da India\n");
                    scanf(" %c", &alternativa);
                    if(alternativa == 'a') {
                        printf("Resposta correta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'b') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'c') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa == 'd') {
                        printf("Resposta incorreta\n");
                        printf("==================================\n");
                    }
                    if(alternativa != 'a') {
                        if(alternativa != 'b') {
                            if(alternativa != 'c') {
                                if(alternativa != 'd') {
                                    printf("Resposta invalida, tente novamente\n");
                                    printf("==================================\n");
                                }
                            }
                        }
                    }
                } while(alternativa != 'a' && alternativa != 'b' && alternativa != 'c' && alternativa != 'd');

                do {
                    printf("Deseja jogar novamente (digite 'j' ) ou retornar para o menu(Digite 'r')?\n");
                    scanf(" %c", &escolhafinal);
                    if(escolhafinal != 'r') {
                        if(escolhafinal != 'j') {
                            printf("Resposta invalida\n");
                        }
                    }
                } while(escolhafinal != 'j' && escolhafinal != 'r');

            } while(escolhafinal == 'j');
        }// if do jogo1
        
        
        if (B == 1) {
                int jogarNovamente;
                do {
                    char nomes[7][7] = {"Kratos", "Ichigo", "Goku", "Luffy", "Naruto", "Seya", "Dante"};
                    char nomeP1[7], nomeP2[7];
                    int caixas[5] = {0, 0, 0, 0, 0};
                    int cobraPos, botaoPos, escolha, vez, fimJogo = 0;
                    int n1, n2, i;

                    printf("\n--- COBRA NA CAIXA ---\nEscolha seu nome da lista:\n");
                    for (i = 0; i < 7; i++) printf("%d. %s  ", i + 1, nomes[i]);

                    do {
                        printf("\nJogador 1 (1-7): ");
                        if (scanf("%d", &n1) != 1) { while (getchar() != '\n'); n1 = 0; }
                    } while (n1 < 1 || n1 > 7);

                    do {
                        printf("Jogador 2 (1-7): ");
                        if (scanf("%d", &n2) != 1) { while (getchar() != '\n'); n2 = 0; }
                    } while (n2 < 1 || n2 > 7 || n1 == n2);

                    strcpy(nomeP1, nomes[n1 - 1]);
                    strcpy(nomeP2, nomes[n2 - 1]);

                    vez = rand() % 2;
                    cobraPos = rand() % 5;
                    do { botaoPos = rand() % 5; } while (botaoPos == cobraPos);
                    caixas[cobraPos] = 1; caixas[botaoPos] = 2;

                    printf("\nO computador sorteou: %s comeca!\n", (vez == 0) ? nomeP1 : nomeP2);

                    while (!fimJogo) {
                        do {
                            printf("\nVez de %s. Escolha uma caixa (1-5): ", (vez == 0) ? nomeP1 : nomeP2);
                            if (scanf("%d", &escolha) != 1) { while (getchar() != '\n'); escolha = 0; }
                        } while (escolha < 1 || escolha > 5);

                        escolha--;
                        if (caixas[escolha] == 1) {
                            printf("POW! Tinha uma COBRA! %s MORREU!\n", (vez == 0) ? nomeP1 : nomeP2);
                            fimJogo = 1;
                        } else if (caixas[escolha] == 2) {
                            printf("CLIQUE! Voce achou o BOTAO! %s VENCEU!\n", (vez == 0) ? nomeP1 : nomeP2);
                            fimJogo = 1;
                        } else if (caixas[escolha] == -1) {
                            printf("Essa caixa ja esta aberta!\n");
                        } else {
                            printf("A caixa %d esta vazia...\n", escolha + 1);
                            caixas[escolha] = -1;
                            vez = 1 - vez;
                        }
                    }
                    printf("\nDeseja jogar novamente? (1- Sim / 0- Voltar pro menu): ");
                    scanf("%d", &jogarNovamente);
                } while (jogarNovamente == 1);
            }
        
        if (C == 1) {
            printf("GOUSMAS WAR\n");
            printf("Furia inicial das suas gousmas(1 e 2) e das do seu oponente(3 e 4):\n gousma1 = 1\n gousma2 = 1\n gousma3 = 1\n gousma4 = 1\n");
            
            g1 = 1;
            g2 = 1;
            g3 = 1;
            g4 = 1;

            //a: atacar\ d: dividir
            while(g1 && g2 != 0 || g3 && g4 != 0){
               printf("====TURNO DO JOGADOR 1====\n");
               atitude = 0;
              
                while (atitude != 'a' && atitude != 'd') {
                	printf("Deseja atacar( digite: a ) ou distribuir sua furia(digite: d)?: ");
                    scanf(" %c", &atitude);
                    if (atitude != 'a') {
                        if (atitude !=  'd') {
                            printf("Opcao indisponivel, tente novamente\n");
                            
                    }
                }

                if (atitude == 'a') {
                	
                    //Escolher quem atacará
                    	atacante = 6;
                    while(atacante != '1' && atacante !='2') {
                    
                	    printf("Qual das suas gousmas ira atacar - gosma1(digite: 1) ou gosma2(digite: 2)?:");
                    	
                    
                        scanf(" %c", &atacante);
                        if (atacante != '1' && atacante !='2') {
                            printf("Opcao indisponivel, tente novamente\n");
                        }
                    }

                    if (atacante == '1') {
                    	
                    	atacado =6;
                        while (atacado != '3' && atacado != '4') {
                        	
                        	
                        	printf("Qual gosma do oponente você deseja atacar- gosma3(digite: 3) ou gosma4(digite: 4)?: ");
                            scanf(" %c", &atacado);
                            if (atacado != '3' && atacado != '4') {
                                printf("Opcao indisponivel, tente novamente\n");
                            }
                        }

                        if (atacado == '3') {
                            g3 += g1;
                            if (g3 > 5) {
                                g3 = 0;
                                printf("gosma3 do oponente foi destruido\n");
                            }
                            if (g4 > 0) { //Vez do computador lutar
                                printf("gosma3 do oponente esta com %d de furia\n", g3);
                                maquina = 32;
                            }
                            if (g3 && g4 == 0) {
                                printf("Você venceu\n");
                            }
                        }

                        if (atacado == '4') {
                            g4 += g1;
                            if (g4 > 5) {
                                g4 = 0;
                                printf("g4 do oponente foi destruido");
                            }
                            if (g4 > 0) { //Vez do computador lutar
                                printf("gosma4 do oponente esta com %d de furia\n", g3);
                            }
                        }
                    }

                    if (atacante == '2') {
                    	atacado =6;
                        while (atacado != '3' && atacado != '4') {
                        	
                        	printf("Qual gosma do oponente você deseja atacar- gosma3(digite: 3) ou gosma4(digite: 4)?: ");
                            scanf(" %c", &atacado);
                            if (atacado != '3' && atacado != '4') {
                                printf("Opcao indisponivel, tente novamente\n");
                            }
                        }

                        if (atacado == '3') {
                            g3 += g1;
                            if (g3 > 5) {
                                g3 = 0;
                                printf("g3 do oponente foi destruido\n");
                            }
                            if (g3> 0) { //Vez do computador lutar
                                printf("gosma3 do oponente esta com %d de furia\n", g3);
                            }
                            if (g3 && g4 == 0) {
                                printf("Você venceu\n");
                            }
                        }

                        if (atacado == '4') {
                            g4 += g1;
                            if (g4 > 5) {
                                g4 = 0;
                                printf("g4 do oponente foi destruido\n");
                            }
                            if (g4 > 0) { //Vez do computador lutar
                                printf("gosma4 do oponente esta com %d de furia\n", g3);
                            }
                            if (0 == g3 && g4) {
                                printf("Você venceu\n");
                            }
                        }
                    }
                }

                if (atitude == 'd') {
                    if (g2 <= 1) {
                        if (g1 <= 1) {
                            printf("Divisao negada: Vida de ambas as gosmas sao insuficientes para doar, tente atacar\n");
                            
                        }
                    }
			       if (g2 > 1 || g1 > 1) {
                         
                            
                            scanf("%d", &doador);
                            if(doador== '1'){
                            	transferidos= 6;
                            	while(transferidos=0 || transferidos>=5){
								
								scanf("%d", &transferidos);
                            	
								}
								
                            	g1-= transferidos;
                            	g2+= transferidos;
                            
							}
                            
                            if(doador== '1'){
                            	transferidos=6;
                            	while(transferidos=0 || transferidos>=5){
								
								scanf("%d", &transferidos);
                            	
								}
                            	
                            	g2-= transferidos;
                            	g1+= transferidos;
                            
                            
                            	
							}
                        
				}
           
        }
      }
      
      //Adicionar o jogo 2
      //Criar um parador para o jogo 3 quando alguem for destruido
	  
                printf("====TURNO DO JOGADOR 2====\n");
                atitude = 0;
              
                while (atitude != 'a' && atitude != 'd') {
                	printf("Deseja atacar( digite: a ) ou distribuir sua furia(digite: d)?: ");
                 scanf(" %c", &atitude);
                    if (atitude != 'a') {
                        if (atitude !=  'd') {
                            printf("Opcao indisponivel, tente novamente\n");
                            
                    }
                }

                if (atitude == 'a') {
                	
                    //Escolher quem atacará
                    	atacante = 6;
                    while(atacante != '3' && atacante !='4') {
                    
                	    printf("Qual das suas gousmas ira atacar - gosma3(digite: 3) ou gosma4(digite: 4)?:");
                    	
                    
                        scanf(" %c", &atacante);
                        if (atacante != '3' && atacante !='4') {
                            printf("Opcao indisponivel, tente novamente\n");
                        }
                    }

                    if (atacante == '3') {
                    	
                    	atacado =6;
                        while (atacado != '1' && atacado != '2') {
                        	
                        	
                        	printf("Qual gosma do oponente você deseja atacar- gosma1(digite: 1) ou gosma1(digite: 2)?: ");
                            scanf(" %c", &atacado);
                            if (atacado != '1' && atacado != '2') {
                                printf("Opcao indisponivel, tente novamente\n");
                            }
                        }

                        if (atacado == '1') {
                            g1 += g3;
                            if (g1 > 5) {
                                g1 = 0;
                                printf("Gousma1 do oponente foi destruido\n");
                            }
                            
                            printf("Sua gousma3 esta com %d de furia\n", g3);
                                
                            
                            if (g1 && g2 == 0) {
                                printf("Você perdeu\n");
                            }
                        }

                        if (atacado == '2') {
                            g2 += g3;
                            if (g2 > 5) {
                                g2 = 0;
                                printf("A sua gousma2 foi destruida");
                            }
                            if (g2 > 0) { //Vez do computador lutar
                                printf("Sua gosma2 esta com %d de furia\n", g3);
                            }
                        }
                    }

                    if (atacante == '4') {
                    	atacado =6;
                        while (atacado != '1' && atacado != '2') {
                        	
                        	printf("Qual gosma do oponente você deseja atacar- gosma1(digite: 1) ou gosma2(digite: 2)?: ");
                            scanf(" %c", &atacado);
                            if (atacado != '1' && atacado != '2') {
                                printf("Opcao indisponivel, tente novamente\n");
                            }
                        }

                        if (atacado == '1') {
                            g1 += g4;
                            if (g1 > 5) {
                                g1 = 0;
                                printf("A sua gousma1 foi destruida\n");
                            }
                            if (g1> 0) { //Vez do computador lutar
                                printf("A sua gousma1 esta com %d de furia\n", g3);
                            }
                            if (g1 && g2 == 0) {
                                printf("Você perdeu\n");
                            }
                        }

                        if (atacado == '2') {
                            g2 += g4;
                            if (g2 > 5) {
                                g2 = 0;
                                printf("A sua gousma2 foi destruida\n");
                            }
                            if (g2 > 0) { //Vez do computador lutar
                                printf("A sua gosma2 esta com %d de furia\n", g3);
                            }
                            if (0 == g3 && g4) {
                                printf("Você perdeu\n");
                            }
                        }
                    }
                }

                if (atitude == 'd') {
                    if (g3 <= 1) {
                        if (g4 <= 1) {
                            printf("Divisao negada: Vida de ambas as gosmas sao insuficientes para doar, tente atacar\n");
                            
                        }
                    }
			       if (g3 > 1 || g4 > 1) {
                         
                            
                            scanf("%d", &doador);
                            if(doador== '3'){
                            	transferidos= 6;
                            	while(transferidos=0 || transferidos>=5){
								
								scanf("%d", &transferidos);
                            	
								}
								
                            	g3-= transferidos;
                            	g4+= transferidos;
                            
							}
                            
                            if(doador== '4'){
                            	transferidos=6;
                            	while(transferidos=0 || transferidos>=5){
								
								scanf("%d", &transferidos);
                            	
								}
                            	
                            	g4-= transferidos;
                            	g3+= transferidos;
                            
            
                            	
							}
                        
			    
		
			}	
			
			}
           
		   }
        }
      
 
      
     }
    } while(escolhafinal == 'r');

    return 0;
}
